function squareСylinder (radiusNumber, heightNumber) {
    const result =2 * Math.PI * radiusNumber * (radiusNumber + heightNumber);
    console.log(result);
    return result;
}
squareСylinder (2,4);